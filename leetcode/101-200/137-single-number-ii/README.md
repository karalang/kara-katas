# 137. Single Number II

> **Difficulty:** Medium &nbsp;·&nbsp; **Topics:** Bit Manipulation · Array &nbsp;·&nbsp; **Source:** [leetcode.com/problems/single-number-ii](https://leetcode.com/problems/single-number-ii/)

Every element appears **three times** except one, which appears once. Find it — in `O(n)` time and `O(1)` extra space.

```
[2,2,3,2]                 ->  3
[0,1,0,1,0,1,99]          -> 99
[-2,-2,1,1,-3,1,-3,-3,-2,30] -> 30
```

**Constraints:** `1 ≤ n ≤ 3·10⁴`, `-2³¹ ≤ nums[i] < 2³¹`, exactly one element appears once.

## Approaches

| Approach | Kāra | Python |
|---|---|---|
| **ones/twos FSM** ★ + **per-bit count mod 3** | [`single_number.kara`](single_number.kara) ✓ | [`single_number.py`](single_number.py) ✓ |

`✓` runs end-to-end today across interpreter, JIT, and codegen (default auto-par and `KARAC_AUTO_PAR=0`), agreeing with the Python mirror. **Both** bit approaches are computed and cross-checked at runtime (they must produce a bit-identical digest); answers are validated against the known LeetCode cases. Zero diagnostics, valgrind-clean.

## The mechanism

Two canonical tricks, both mirrored:

- **ones/twos FSM** (★): a per-bit 2-bit counter mod 3, carried in two 32-bit masks. `ones ^= x & ~twos; twos ^= x & ~ones` — after all elements, a bit set 3k times cancels to `00`, and the bit set once lands in `ones`. Sign-extended from 32 bits.
- **Per-bit count mod 3**: for each of the 32 bit positions, count how many numbers have it set; a residue `≠ 0 mod 3` is a bit of the answer.

Running both and asserting they agree is a self-check that also exercises the full bitwise operator set.

## Kāra features exercised

- **Full bitwise operator set** — `&`, `|`, `^`, `~`, `<<`, `>>` on `i64`, verified interp==build==Python (`12 & 10 = 8`, `~12 = -13`, `12 << 2 = 48`, …). The first corpus kata to lean on bit-twiddling.
- **32-bit two's-complement masking + sign extension** in i64 — `x & 0xFFFFFFFF`, then subtract `2³²` when the top bit is set.
- **`ref Vec[i64]` read-only parameters** — the two helpers borrow the same `nums` (see the note below).

> **Compiler friction surfaced & fixed by this kata.** Writing the two helpers as `fn helper(nums: Vec[i64])` (owned) and calling both on the same `nums` makes `karac check` report `value 'nums' moved here, used again here` — while `karac build`/`run` compile and run it correctly (the reuse is defensive-copied). That split is *by design* (Kāra's `owned = move` semantics is load-bearing, and ownership errors other than aliased exclusive borrows are advisory for build). The real gap was that the diagnostic wasn't machine-fixable, so `karac fix` couldn't repair it in the Mend loop. Fixed in the compiler ([kara `B-2026-07-19-3`](https://github.com/karalang/kara/blob/main/docs/bug-ledger.jsonl)): the `UseAfterMove` error now carries a gated `.clone()` auto-fix (`f(v)` → `f(v.clone())`) for clone-able types (String/Vec/Map/Set/RC), so `karac fix` repairs it. The idiomatic declaration for a read-only function is `ref Vec[i64]` anyway, which this solver uses.

## Benchmarks
<!-- bench-staleness -->
> **Figures in this section are undated; the feed was last measured 2026-08-17.** Where the two disagree, [`bench/results.json`](bench/results.json) and the [charts](../../../BENCHMARKS.md) are current; the numbers below are kept because the analysis around them explains *why* the shape is what it is, and that reasoning outlives the milliseconds.

[`bench/`](bench/) — `bash bench/bench.sh` (generated by
`scripts/new-bench.sh`). Read
[`../../../BENCHMARKS.md`](../../../BENCHMARKS.md) before quoting any of these.

> **Host:** the tables below are a shared **x86-64 Linux cloud container**
> snapshot, kept as `bench/results.container-x86.json`. The canonical Apple M5
> Pro lane is [`bench/results.json`](bench/results.json) — that is the file
> `scripts/consolidate-bench.sh` feeds into the top-level chart, and it is
> current as of the date stamped above. Absolute milliseconds are NOT comparable
> between the two hosts; only the **within-file cross-language ratios** are.

**Workload.** Build-once + punch: four arrays of 120,001 `i64` (30,000 LCG
values interleaved three times, plus one loner) built once, then 40 punches.
Each punch runs **both** bit tricks over one array and cross-checks that they
agree, so a divergence lands in the sink instead of passing silently. Variant B
does ~32× the work of A on identical data, so the measurement is dominated by
its nested per-bit counting loop. Sink = `79998760`, identical in all five
languages.

### Runtime — 30 runs, 5 warmup

| Lane | mean ± σ | vs kāra |
|---|---|---|
| c | 35.2 ms ± 1.4 | 8.00× faster |
| rust | 37.4 ms ± 3.1 | 7.53× faster |
| go | 112.7 ms ± 10.3 | 2.50× faster |
| rust (overflow-checks=on) | 277.2 ms ± 8.9 | 1.01× faster |
| **kāra** | **281.3 ms ± 4.4** | — |

**This is the sharpest equal-safety result in the corpus.** Kāra ties the
equal-safety Rust build to within 1.5% — inside σ — and both sit **7.9×**
behind the two builds that are allowed to wrap silently. Nothing about the
generated code differs in kind between kāra and `rust_ovf`; they lose the same
7.9× to the same cause.

### The whole 7.9× is one overflow check — measured in three steps

The hot loop is variant B's innermost statement:

```kara
if ((nums[i] >> b) & 1i64) == 1i64 {
    cnt = cnt + 1i64;
}
```

1. **It is that one check, not checked arithmetic in general.** Making *only*
   `cnt += 1` unchecked in the Rust mirror (`wrapping_add`) — with
   `overflow-checks=on` still applying to `i`, `b`, and every expression in
   `main` — takes it from **277 ms to 30 ms**. One statement accounts for the
   entire gap.
2. **It is not vectorization.** Rebuilding that same unchecked variant with
   LLVM's loop and SLP vectorizers disabled
   (`-C llvm-args=-vectorize-loops=false -C llvm-args=-vectorize-slp=false`)
   still runs in **40 ms**. The fast path is fast scalar code, not SIMD.
3. **It is branch misprediction.** Replacing the LCG values with a constant, so
   `(nums[i] >> b) & 1` is uniform for each bit position and the branch is
   perfectly predicted, collapses **both** checked builds: `rust_ovf`
   277 → **45 ms**, kāra 281 → **50 ms**.

The mechanism: a checked `cnt + 1` can trap, so it cannot be speculated, so
LLVM cannot if-convert `if bit { cnt += 1 }` into the branchless
`cnt += bit`. What remains is a real conditional branch on a ~50%-random bit —
mispredicted about half the time, on 154 million elements.

This is a **standing optimization opportunity for `karac`**, not a fact of
checked arithmetic: `cnt` is a loop counter incremented at most `n` times, so
its bound is provable and the check is removable, which would restore
if-conversion. Filed as a perf item in the [`karac` bug
ledger](../../../../kara/docs/bug-ledger.md).

### Compile, size, memory

| Metric | kāra | rust | c | go |
|---|---|---|---|---|
| Compile (cold) | 250.5 ms ± 17.0 | 134.2 ms ± 3.6 | 88.4 ms ± 3.0 | — |
| Binary size | **373.6 KiB** | 3864.6 KiB | 15.8 KiB | 2166.6 KiB |
| Runtime peak RSS | 5.1 MiB | 5.1 MiB | 4.5 MiB | 5.6 MiB |
| Compile peak RSS | **91.7 MiB** | 108.6 MiB | 97.4 MiB | — |

### Mirror-parity notes

- All five mirrors run the same two functions over the same arrays and print
  the same sink; the C version stores the arrays statically rather than heap
  allocating, which is the only structural difference and costs nothing here
  (the build phase is ~1% of the measurement).
- **Go** uses `&^` (AND NOT) for the `& ~x` in the FSM — the direct spelling of
  the same operation, not a different one.
- Python is present as a correctness oracle only; it is absent from the feed
  (`KARA_BENCH_INCLUDE_PY` defaults to `0`).

## Running

```bash
karac run   single_number.kara
karac build single_number.kara && ./single_number
python3 single_number.py
diff <(karac run single_number.kara) <(python3 single_number.py) && echo OK
```

## Notes

Dogfood-first bit-manipulation kata: it verifies the full `i64` bitwise operator set is codegen-correct (interp==build), cross-checks two independent bit tricks at runtime, and surfaced the check-vs-build ownership divergence `B-2026-07-19-3`.
