# 132. Palindrome Partitioning II

> **Difficulty:** Hard &nbsp;·&nbsp; **Topics:** DP · String &nbsp;·&nbsp; **Source:** [leetcode.com/problems/palindrome-partitioning-ii](https://leetcode.com/problems/palindrome-partitioning-ii/)

Return the **minimum** number of cuts needed so every piece of `s` is a palindrome.

```
"aab"   ->  1   (aa | b)
"aabaa" ->  0   (already a palindrome)
"abcde" ->  4   (a|b|c|d|e)
```

**Constraints:** `1 ≤ s.length ≤ 2000`, lowercase.

## Approaches

| Approach | Complexity | Kāra | Python |
|---|---|---|---|
| **O(n²) palindrome-table DP** ★ | O(n²) time & space | [`min_cut.kara`](min_cut.kara) ✓ | [`min_cut.py`](min_cut.py) ✓ |

`✓` runs end-to-end today: interpreter, JIT, and codegen (default auto-par and `KARAC_AUTO_PAR=0`) all agree with the Python mirror. Validated against known values (aab 1, aabaa 0, abcde 4) plus deterministic-LCG strings. Zero diagnostics, valgrind-clean.

## The mechanism

Two DPs. First a boolean table `pal[i][j]` — filled by increasing substring length, `s[i..=j]` is a palindrome iff its ends match and the inner span `pal[i+1][j-1]` is (with the length-2 base case). Then `cut[i]` = minimum cuts for the prefix `s[0..=i]`: `0` when the whole prefix is already a palindrome, else `min(cut[j-1] + 1)` over every `j` where `s[j..=i]` is a palindrome. The answer is `cut[n-1]`. The DP sibling of [#131](../131-palindrome-partitioning/) — where that enumerates every partition, this collapses to one min-cut scalar via a table.

## Benchmarks

[`bench/`](bench/) — `bash bench/bench.sh` (generated by
`scripts/new-bench.sh`). Read
[`../../../BENCHMARKS.md`](../../../BENCHMARKS.md) before quoting any of these.

> **Host:** shared **x86-64 Linux cloud container**, committed as
> `bench/results.container-x86.json`, not `results.json` — the latter is
> reserved for canonical Apple M5 Pro numbers and is the only file
> `scripts/consolidate-bench.sh` feeds into the top-level chart. This kata has
> no M5 run, so it is deliberately absent from the consolidated feed. Only the
> **within-file cross-language ratios** are comparable.

**Workload.** Build-once + punch: four 500-character case strings built once,
then 400 punches of the full O(n²) DP. Sink = `89300`.

**Alphabet size is the knob.** The four cases use alphabets of size 2, 4, 26
and 3. A 2-letter alphabet makes the palindrome table dense, so the `cut` loop
scans many `true` entries; 26 letters make almost nothing a palindrome and the
inner loop exits early. Spanning both means neither branch of `if pal[0][i]`
dominates the measurement.

### Runtime — 30 runs, 5 warmup

| Lane | mean ± σ | vs kāra |
|---|---|---|
| c | 245.6 ms ± 6.5 | 1.31× faster |
| **kāra** | **322.0 ms ± 8.6** | — |
| rust | 338.9 ms ± 12.2 | 1.05× slower |
| go | 351.6 ms ± 9.5 | 1.09× slower |
| rust (overflow-checks=on) | 351.8 ms ± 13.6 | 1.09× slower |

Kāra edges out both Rust (1.05×) and Go (1.09×) and sits 1.31× behind
`clang -O3`. Overflow checks cost Rust 4% (338.9 → 351.8 ms).

### The point of benching this alongside #131

These two katas are the **same problem with different algorithms**, and they
land on opposite sides of kāra's profile:

| | [#131](../131-palindrome-partitioning/README.md) | #132 (this kata) |
|---|---|---|
| Algorithm | enumerate every partitioning (exponential backtracking) | minimum cut count (O(n²) DP) |
| Dominant cost | **per-piece `String` allocation** | dense 2-D table fill, no inner-loop allocation |
| kāra vs rust | **1.30× slower** | **1.05× faster** |
| kāra rank | last of four | second of four |

Same problem, same repo, same host — and reversing the algorithm reverses the
result. That is the clearest single demonstration in the corpus of where kāra's
deficit actually lives: not in loops, arithmetic, or table indexing, but in
**heap-`String` churn**. #332, #290, #291 and #131 all agree on the slow side;
#238, #240, #235, #129, #130 and this kata all agree on the fast side.

### Mirror-parity note

All four lanes use a **genuinely nested boolean table** — row pointers in C,
`Vec<Vec<bool>>` in Rust, `[][]bool` in Go — rather than a flat `n*n` block. C
additionally uses `unsigned char` rather than a packed bitset; a bitset would
be a different data structure from the other lanes' bool vectors and would have
handed C a large and unrepresentative advantage on the `pal[j][i]` scan.

### Compile, size, memory

| Metric | kāra | rust | c | go |
|---|---|---|---|---|
| Compile (cold) | 326.7 ms ± 84.5 | 180.5 ms ± 4.5 | 116.2 ms ± 6.9 | — |
| Binary size | **337.0 KiB** | 3866.7 KiB | 15.9 KiB | 2166.5 KiB |
| Runtime peak RSS | 2.6 MiB | 2.4 MiB | 1.8 MiB | 6.9 MiB |
| Compile peak RSS | **90.4 MiB** | 115.8 MiB | 98.3 MiB | — |

The Python mirror is the correctness oracle, not a measured lane — it stays
behind the `KARA_BENCH_INCLUDE_PY` gate by design.

## Kāra features exercised

- **`Vec[Vec[bool]]` DP table with 2-D index-assign** — `pal[lo][hi] = true` in-place, read as `pal[i+1][j-1]`; a boolean-element nested Vec (distinct from [#130](../130-surrounded-regions/)'s `Vec[Vec[i64]]`).
- **`Vec[i64]` cut array** with index-assign `cut[i] = best` and back-references `cut[j-1]`.
- **Length-ordered table fill** — the classic interval-DP loop order (`length` outer, `lo` inner) so `pal[i+1][j-1]` is always resolved before `pal[i][j]`.
- **Overflow-checked byte compares** over `s.bytes()`.

## Running

```bash
karac run   min_cut.kara
karac build min_cut.kara && ./min_cut
python3 min_cut.py
diff <(karac run min_cut.kara) <(python3 min_cut.py) && echo OK
```

## Notes

Dogfood-first interval-DP kata exercising `Vec[Vec[bool]]` 2-D index-assign and length-ordered table fill across every surface.
