diff --git a/src/codegen.rs b/src/codegen.rs
index 381572f1..2149a8ab 100644
--- a/src/codegen.rs
+++ b/src/codegen.rs
@@ -2817,6 +2817,20 @@ pub(super) struct Codegen<'ctx> {
     /// `compile_function`) and the call-site arg-inc skip (by position,
     /// in the direct-call arg loop) — both gated on `headerless_types`.
     pub(crate) borrowed_param_skips: HashMap<String, Vec<(String, usize, String)>>,
+    /// PROTOTYPE (env `KARAC_RC_ELIDE_REF_PARAMS`, default OFF): per-fn
+    /// `(param name, position)` of every parameter the ownership pass
+    /// classified as `ref`/borrow (`OwnershipMode::Ref`). ORed into the
+    /// same call-site arg-inc skip (by position) and callee-side exit-dec
+    /// skip (by name) as `borrowed_param_skips` — but WITHOUT the
+    /// `headerless_types` guard, so it fires for any `Option[shared]` /
+    /// `shared` borrow param. The skip machinery it drives is the fully
+    /// plumbed, LSan-verified C2b borrow path: no arg inc, no
+    /// transfer/consume of the source, no callee exit dec — a pure
+    /// balanced borrow, sound because the ownership pass proved the param
+    /// is only read (never mutated/consumed/escaped) so the caller keeps
+    /// the referent alive across the call. Empty unless the env flag is
+    /// set. See docs/bug-ledger (RC-elide-ref PERF entry).
+    pub(crate) rc_elide_ref_params: HashMap<String, Vec<(String, usize)>>,
     /// Per-function Arc-promoted binding names — the subset of `rc_fallback_fns`
     /// flagged by the ownership pass as crossing a `par {}` thread boundary.
     /// Inc/dec on these bindings emits atomic LLVM operations (`atomicrmw add` /
@@ -6044,6 +6058,7 @@ impl<'ctx> Codegen<'ctx> {
             headerless_types: HashSet::new(),
             conditional_adopted_roots: HashMap::new(),
             borrowed_param_skips: HashMap::new(),
+            rc_elide_ref_params: HashMap::new(),
             arc_fallback_fns: HashMap::new(),
             rc_fallback_heap_types: HashMap::new(),
             rc_fallback_box_drop_fns: Vec::new(),
@@ -6204,6 +6219,28 @@ impl<'ctx> Codegen<'ctx> {
             self.arc_fallback_fns
                 .insert(fn_name.clone(), arc_set.clone());
         }
+        // PROTOTYPE RC-elide-ref (env `KARAC_RC_ELIDE_REF_PARAMS`, default
+        // OFF): harvest every parameter the ownership pass classified
+        // `ref`/borrow into `rc_elide_ref_params`, so `borrowed_arg_skip`
+        // / `borrowed_param_dec_skip` treat it as a pure balanced borrow
+        // (no arg inc, no callee exit dec). The classification is the same
+        // one that drives the `karac query ownership` "mode: ref" report
+        // and the RC-fallback soundness — a `Ref` param is only read,
+        // never mutated/consumed/escaped, so the caller keeps the referent
+        // live for the call and the balanced inc/dec pair is deletable.
+        if std::env::var_os("KARAC_RC_ELIDE_REF_PARAMS").is_some() {
+            for (fn_name, modes) in &ow.param_modes {
+                let recs: Vec<(String, usize)> = modes
+                    .iter()
+                    .enumerate()
+                    .filter(|(_, (_, m))| matches!(m, crate::ownership::OwnershipMode::Ref))
+                    .map(|(i, (n, _))| (n.clone(), i))
+                    .collect();
+                if !recs.is_empty() {
+                    self.rc_elide_ref_params.insert(fn_name.clone(), recs);
+                }
+            }
+        }
         // RC elision phase A: per-fn elided-binding sets. Consulted by
         // the let-stmt shared arm via `is_elided_binding`.
         for (fn_name, names) in &ow.elided_bindings {
@@ -6497,6 +6534,14 @@ impl<'ctx> Codegen<'ctx> {
             recs.iter()
                 .any(|(_, pos, t)| *pos == position && self.headerless_types.contains(t))
         })
+        // PROTOTYPE RC-elide-ref: the callee's param at `position` was
+        // classified `ref`/borrow by the ownership pass → skip the
+        // call-site arg inc (the callee's exit dec is skipped
+        // symmetrically in `borrowed_param_dec_skip`).
+        || self
+            .rc_elide_ref_params
+            .get(callee)
+            .is_some_and(|recs| recs.iter().any(|(_, pos)| *pos == position))
     }
 
     /// Phase C2b: the callee-side half — `param_name` of the CURRENT fn
@@ -6509,6 +6554,13 @@ impl<'ctx> Codegen<'ctx> {
                 recs.iter()
                     .any(|(n, _, t)| n == param_name && self.headerless_types.contains(t))
             })
+        // PROTOTYPE RC-elide-ref: the current fn's `param_name` was
+        // classified `ref`/borrow → skip its exit dec (the caller
+        // skipped the arg inc symmetrically in `borrowed_arg_skip`).
+        || self
+            .rc_elide_ref_params
+            .get(&self.current_fn_name)
+            .is_some_and(|recs| recs.iter().any(|(n, _)| n == param_name))
     }
 
     /// Phase-B2 role lookup for the current function.
