# 238. Product of Array Except Self

Return an array `out` where `out[i]` is the product of every element of `nums`
**except** `nums[i]` — **without division**, in `O(n)` time and `O(1)` extra
space (the output array is not counted against the space bound).

## Approach

Two sweeps over the output array itself:

1. **left → right:** `out[i]` receives the product of everything strictly *left*
   of `i` (a running prefix product).
2. **right → left:** multiply each `out[i]` by a running product of everything
   strictly *right* of `i` (a suffix product folded in).

After both passes `out[i] = (∏ left of i) × (∏ right of i) = ∏ except i`. Zeros
fall out for free: one zero zeroes every other position; two zeros zero
everything.

## Files

- [`product_except_self.kara`](product_except_self.kara) — Kāra implementation.
- [`product_except_self.py`](product_except_self.py) — Python mirror (oracle).

Expected output (both):

```
24 12 8 6
0 0 9 0 0
12 8 6
9 5
```

## Benchmarks
<!-- bench-staleness -->
> **Figures in this section are undated; the feed was last measured 2026-07-27.** Where the two disagree, [`bench/results.json`](bench/results.json) and the [charts](../../../BENCHMARKS.md) are current; the numbers below are kept because the analysis around them explains *why* the shape is what it is, and that reasoning outlives the milliseconds.

[`bench/`](bench/) — `bash bench/bench.sh` (generated by
`scripts/new-bench.sh`). Read
[`../../../BENCHMARKS.md`](../../../BENCHMARKS.md) before quoting any of these.

> **Host:** shared **x86-64 Linux cloud container**, committed as
> `bench/results.container-x86.json`, not `results.json` — the latter is
> reserved for canonical Apple M5 Pro numbers and is the only file
> `scripts/consolidate-bench.sh` feeds into the top-level chart. This kata has
> no M5 run, so it is deliberately absent from the consolidated feed. Only the
> **within-file cross-language ratios** are comparable.

**Workload.** Build-once + punch: 8 arrays of 100 000 values built once, then
400 punches of the two-sweep prefix/suffix pass. Sink = `-331405379`, identical
across all five languages.

### Runtime — 30 runs, 5 warmup

| Lane | mean ± σ | vs kāra |
|---|---|---|
| c | 273.7 ms ± 7.3 | 1.02× faster |
| **kāra** | **279.9 ms ± 7.0** | — |
| rust | 280.0 ms ± 9.3 | **1.00× — exact tie** |
| rust (overflow-checks=on) | 290.5 ms ± 14.7 | 1.04× slower |
| go | 557.7 ms ± 191.9 | 1.99× slower |

**The strongest kāra result in the corpus.** It ties Rust to within 0.1 ms
(279.9 vs 280.0) and lands **within 2% of `clang -O3`** — on a workload where
the three are doing genuinely identical work: two linear sweeps over a flat
`i64` array with one multiply and one store per element. There is no hash, no
string, no allocation asymmetry left to explain a gap, and none appears.

Overflow checks cost Rust 3.7% here (280.0 → 290.5 ms) — visible but small,
since the multiplies are `±1` and the loop is memory-bound rather than
ALU-bound. Contrast [#242](../242-valid-anagram/README.md), where the same
flag cost 1.87× on an arithmetic-bound inner loop.

**Go's σ is 34%** (191.9 ms on a 557.7 ms mean) — by far the noisiest lane in
the corpus. Its 1.99× deficit is real but the figure should be read loosely.

### Three workload-design decisions, all load-bearing

This kata took three attempts to size honestly, and each failure would have
produced a benchmark that looked fine:

1. **Values are bounded to `{-1, 1}`.** The algorithm multiplies a running
   product across the whole array, so arbitrary values overflow `i64` within
   ~20 elements. Kāra traps integer overflow by default, so the benchmark would
   have aborted rather than measured. LeetCode's own constraint guarantees the
   product fits in 32 bits, so a bounded-product input is representative.
2. **Zeros are excluded.** With `{-1, 0, 1}`, a 100 000-element draw contains
   two zeros with near-certainty, which zeroes every output and collapses the
   sink to `0` — an oracle a completely broken implementation would also
   satisfy. The kata's own tests cover the zero path.
3. **Values come from LCG bit 16, not bit 0.** A power-of-two-modulus LCG has
   **period 2 in its low bit**, so `x % 2` produces a perfectly alternating
   `1,-1,1,-1,…`. With 8 arrays whose starting parity splits evenly, the
   position-weighted sums then cancel to *exactly* zero — the sink was still
   `0` after fixing (2), for an entirely different reason.

### A cross-language `%` trap

The sink is negative, and **Python's `%` floors while Kāra, Rust, C and Go all
truncate toward zero.** Using Python's operator directly makes the oracle
disagree with the other four on a perfectly correct computation. The Python
mirror applies an explicit `trunc_mod`; all five then agree.

### Compile, size, memory

| Metric | kāra | rust | c | go |
|---|---|---|---|---|
| Compile (cold) | 366.0 ms ± 13.8 | 225.0 ms ± 8.3 | 124.6 ms ± 9.5 | — |
| Binary size | **332.8 KiB** | 3864.1 KiB | 15.7 KiB | 2162.2 KiB |
| Runtime peak RSS | 8.9 MiB | 8.9 MiB | 8.4 MiB | 16.4 MiB |
| Compile peak RSS | **90.0 MiB** | 116.5 MiB | 95.9 MiB | — |

The Python mirror is the correctness oracle, not a measured lane — it stays
behind the `KARA_BENCH_INCLUDE_PY` gate by design.

