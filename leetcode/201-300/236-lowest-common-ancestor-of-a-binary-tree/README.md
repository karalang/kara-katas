# 236. Lowest Common Ancestor of a Binary Tree

Given a binary tree and two present values `p` and `q`, return the value of their
**lowest common ancestor** — the deepest node having both as descendants (a node
is a descendant of itself).

## Approach

Unlike [#235](../235-lowest-common-ancestor-of-a-bst/) there is **no BST order**
to steer the search, so each node must inspect *both* subtrees. Classic recursive
post-order — each node reports what it found beneath it:

- an empty subtree, or a node that **is** `p` or `q`, reports itself upward;
- a node that gets a non-null answer from **both** children is the split point →
  it is the LCA;
- a node with only one non-null child answer forwards that answer up.

The first node to see the two targets arrive from different subtrees (or to be
one target with the other beneath it) is the LCA. `O(n)` time, `O(h)` stack.

The tree is an **index pool** (`Vec[Node]`, `i64` `left`/`right`, `-1` = null),
built from explicit `(val, left, right)` parallel arrays so *any* shape — not
just a BST — can be described.

## Files

- [`lca_binary_tree.kara`](lca_binary_tree.kara) — Kāra implementation.
- [`lca_binary_tree.py`](lca_binary_tree.py) — Python mirror (oracle).

Expected output (both): `3 5 5 2 3 3 1` (one per line).

<!-- placement-caveat -->
**Measurement caveat — code placement.** This kata's runtime moves by up to **16%** with code placement alone: rebuilt with its machine code sitting at a different address, the same program, same compiler and same input runs that much faster or slower. That is wider than the **2.8%** margin against `go build` quoted below, so read that comparison as a tie rather than as a result. Measured across four code placements against a same-binary control — see [`placement-spread.json`](../../../placement-spread.json) and [BENCHMARKS.md](../../../BENCHMARKS.md#code-placement-arm64).

## Benchmarks
<!-- bench-staleness -->
> **Figures in this section are undated; the feed was last measured 2026-08-17.** Where the two disagree, [`bench/results.json`](bench/results.json) and the [charts](../../../BENCHMARKS.md) are current; the numbers below are kept because the analysis around them explains *why* the shape is what it is, and that reasoning outlives the milliseconds.
> Comparative claims below ("ahead of C", "leads Rust", ratios) were true of the snapshot and have **not** been re-verified against the current feed — treat them as historical, not as the standing result.

[`bench/`](bench/) — `bash bench/bench.sh` (generated by
`scripts/new-bench.sh`). Read
[`../../../BENCHMARKS.md`](../../../BENCHMARKS.md) before quoting any of these.

> **Host:** the tables below are a shared **x86-64 Linux cloud container**
> snapshot, kept as `bench/results.container-x86.json`. The canonical Apple M5
> Pro lane is [`bench/results.json`](bench/results.json) — that is the file
> `scripts/consolidate-bench.sh` feeds into the top-level chart, and it is
> current as of the date stamped above. Absolute milliseconds are NOT comparable
> between the two hosts; only the **within-file cross-language ratios** are.

**Workload.** Build-once + punch: one 100 000-node complete binary tree built as
an index pool, then 600 full recursive post-order LCA searches. Sink = `4189`.

**The contrast with [#235](../235-lowest-common-ancestor-of-a-bst/README.md) is
the point of benching both.** There, BST ordering lets the walk pick a direction
and finish in O(h) ≈ 35 steps, so it needs **8 000 000** queries to fill a
comparable runtime. Here there is no ordering to exploit, the recursion must
inspect *both* subtrees, and the cost is O(n) per query — so **600** queries
suffice. Same problem, same tree representation, three orders of magnitude apart
in per-query cost.

### Runtime — 30 runs, 5 warmup

| Lane | mean ± σ | vs kāra |
|---|---|---|
| c | 309.1 ms ± 33.3 | 1.21× faster |
| go | 325.1 ms ± 36.2 | 1.15× faster |
| **kāra** | **375.2 ms ± 52.3** | — |
| rust (overflow-checks=on) | 429.3 ms ± 36.5 | 1.14× slower |
| rust | 507.7 ms ± 88.7 | 1.35× slower |

**Kāra beat both Rust configurations** on recursive tree traversal *in this
container snapshot*. **That is no longer the standing result** — on the current
M5 feed kāra is 1.17× *behind* the overflow-checked Rust. The reversal is not
the workload: holding the compiler fixed and varying only the generator width
leaves kāra's time unchanged to three digits, and the C, Rust and Go lanes were
flat across the same re-measurement. It is a kāra-only regression between
2026-07-27 and 2026-08-17, tracked as `B-2026-08-17-36` and not yet bisected.
The container analysis below is kept because it explains the *shape* of the
lane, which has not changed.

Because the σ on that first run was large (14–17%), the lanes were re-measured
with 50 runs and warmup 8:

| Lane | mean ± σ | median |
|---|---|---|
| c | 302.4 ms ± 41.9 | 288.1 ms |
| kāra | 343.0 ms ± 14.8 | **339.7 ms** |
| rust (overflow-checks=on) | 401.7 ms ± 24.6 | 394.0 ms |
| rust | 460.8 ms ± 46.0 | 450.5 ms |

On medians, *on that container*: kāra is **1.33× faster than `rustc -O`**,
**1.16× faster than the overflow-checked Rust**, and 1.18× behind `clang -O3`.
On the current M5 feed both Rust figures are ahead of kāra instead — see the
regression note above.

### An anomaly that reproduced: `rust_ovf` faster than `rust`

In both independent runs, Rust **with** overflow checks measured *faster* than
Rust without — 429.3 vs 507.7 ms, then 394.0 vs 450.5 ms on medians. Enabling a
check cannot make code faster, so on [#235](../235-lowest-common-ancestor-of-a-bst/README.md)
a similar 7% inversion was correctly dismissed as noise.

Here it is **13% and it reproduces across 80 measured runs**, which noise does
not. The plausible reading is a code-layout artifact: `-C overflow-checks=on`
changes inlining and block placement, and compiled performance is not monotonic
in added instrumentation — an unlucky layout for the plain build is entirely
possible on a recursive function this small.

The practical consequence for this table: the plain `rust` lane should be
treated as a slightly unlucky build, and **`rust_ovf` at 394 ms is the fairer
Rust figure** to compare kāra's 340 ms against. Kāra is ahead either way, which
is why both are shown rather than only the flattering one.

### Compile, size, memory

| Metric | kāra | rust | c | go |
|---|---|---|---|---|
| Compile (cold) | 329.3 ms ± 25.3 | 147.5 ms ± 12.4 | 105.5 ms ± 6.6 | — |
| Binary size | **332.7 KiB** | 3863.7 KiB | 15.7 KiB | 2162.1 KiB |
| Runtime peak RSS | 4.5 MiB | 4.4 MiB | 3.9 MiB | 4.0 MiB |
| Compile peak RSS | **90.8 MiB** | 110.7 MiB | 96.0 MiB | — |

All four lanes use an index-pool tree rather than pointer-linked nodes, so no
lane gets an allocator or locality advantage.

The Python mirror is the correctness oracle, not a measured lane — it stays
behind the `KARA_BENCH_INCLUDE_PY` gate by design. Its parity was verified at
reduced scale: `N=5000, ITERS=30` gives sink `2868` from both kāra and Python.

