# 347. Top K Frequent Elements

> **Difficulty:** Medium &nbsp;·&nbsp; **Topics:** Hash Map, Sorting, Counting &nbsp;·&nbsp; **Source:** [leetcode.com/problems/top-k-frequent-elements](https://leetcode.com/problems/top-k-frequent-elements/)

Given an integer array `nums` and an integer `k`, return the `k` most frequent
elements.

```
nums = [1,1,1,2,2,3], k = 2  →  [1,2]
nums = [1],           k = 1  →  [1]
```

**Constraints:** `1 ≤ nums.length ≤ 10⁵`; `k` is in `[1, number of distinct
elements]`. LeetCode accepts the answer in **any order**; both implementations
here impose a total order — **count descending, then key ascending** — so the
output is deterministic and directly diffable against the oracle.

## Approaches

Both files run the *same* three steps: tally into a `Map`, recover the distinct
keys with a `keys()` walk, insertion-sort by `(count desc, key asc)`, take `k`.
Only the map's halves differ, and that is the entire point.

| Approach | File | Map halves |
|---|---|---|
| **Scalar-keyed** ★ | [`top_k_frequent.kara`](top_k_frequent.kara) | `Map[i64, i64]` — both halves scalar |
| String-keyed | [`top_k_frequent_words.kara`](top_k_frequent_words.kara) | `Map[String, i64]` — heap key half |
| Bounded heap | [`top_k_frequent_heap.kara`](top_k_frequent_heap.kara) | `PriorityQueue[Weakest]` — a user-defined `Ord`, because the answer's two sort keys disagree on direction |
| Reference oracles | [`top_k_frequent.py`](top_k_frequent.py), [`top_k_frequent_words.py`](top_k_frequent_words.py), [`top_k_frequent_heap.py`](top_k_frequent_heap.py) | known-correct answers |

## Why this kata

Chosen by **compiler surface, not sequence** — and specifically to put live
coverage under an **open** ledger entry.

`keys()` lowers two different ways depending on the map's halves. When both
halves are scalar it becomes an inline bucket walk with no runtime iterator
call (ledger B-2026-07-24-2, `bef6bbc`). When either half is heap-allocated it
does not, and still materializes an intermediate keys Vec — **B-2026-07-25-4,
still open**.

Until now the corpus only covered the fast side (#387 exercises scalar
`keys()`). This kata covers both halves of that fork with one algorithm, so the
two files must agree on every answer; any divergence is a compiler bug rather
than an algorithm difference.

The split is visible in the linked binaries — the scalar build never pulls the
runtime iterator in at all:

```console
$ nm top_k_frequent       | grep -c karac_map_iter     # scalar halves
0
$ nm top_k_frequent_words | grep -c karac_map_iter     # heap key half
3     # karac_map_iter_new / _next / _free
```

No new bugs surfaced. Both variants were correct on the first compile and agree
with their oracles across all four surfaces, which is the useful signal: the
heap-half path is *slow*, not *wrong*.

## Verification

| Surface | `top_k_frequent` | `top_k_frequent_words` |
|---|---|---|
| `karac run --interp` | ✅ | ✅ |
| `karac run` (LLJIT) | ✅ | ✅ |
| `karac build` (auto-par default) | ✅ | ✅ |
| `karac build` + `KARAC_AUTO_PAR=0` | ✅ | ✅ |

Eight checks, all byte-identical to the Python oracles — including the
count-tie cases (resolved by key ascending), the all-counts-equal case, and
`k` larger than the number of distinct keys.

## Benchmarks
<!-- bench-staleness -->

[`bench/`](bench/) — `bash bench/bench.sh` (generated by
`scripts/new-bench.sh`). Read
[`../../../BENCHMARKS.md`](../../../BENCHMARKS.md) before quoting any of these.

> **Host:** canonical **Apple M5 Pro**, committed as
> [`bench/results.json`](bench/results.json) and fed into the top-level chart by
> `scripts/consolidate-bench.sh`. The older x86-64 Linux container run is kept
> alongside as `bench/results.container-x86.json`, a corroborating second host
> rather than a source of published claims. Absolute times are not comparable
> across hosts; only the **within-file cross-language ratios** are.

**Workload.** Build-once + punch: an 8000-element array over 200 distinct keys
is built once (every 5th slot drawn from a 13-key pool, so the top-k answer is
well defined rather than an arbitrary tie), then 300 punches each mutate one
element and re-run the full tally + `keys()` walk + `(count desc, key asc)`
insertion sort with `k=10`. Sink = `18900`, identical across all five
languages. Because the sort key is a *total* order over distinct keys, the
sorted result is unique regardless of each language's map iteration order —
that is what makes one sink comparable across five different map
implementations.

Only the scalar-keyed variant is benched. The String-keyed sibling is a
correctness/codegen-surface probe for an open bug, not a performance claim.

### Runtime — 30 runs, 5 warmup

Apple M5 Pro, karac `0.1.0-dev.8508+gc9032a670`, measured 2026-09-08.

| Lane | mean ± σ | vs kāra |
|---|---|---|
| c | 5.3 ms ± 0.4 | **8.35× faster** |
| go | 33.1 ms ± 0.9 | 1.33× faster |
| rust (overflow-checks=on) | 37.2 ms ± 0.1 | 1.18× faster |
| rust | 37.5 ms ± 0.2 | 1.17× faster |
| **kāra** | **44.0 ms ± 1.0** | — |

### The hash caveat this README used to carry is now RESOLVED — and it was right

Every earlier revision of this section warned that kāra's margin over Rust was
mostly the hash function: kāra hashed an integer key with a single Fibonacci
multiply while Rust's `HashMap` used DoS-resistant SipHash-1-3, so the two were
never doing the same work. [`bench/top_k_frequent_fasthash.rs`](bench/top_k_frequent_fasthash.rs)
existed to put Rust on the same multiply, and it measured a **statistical tie**
(kāra 1.10 ± 0.35× — an interval comfortably spanning 1.0), concluding that
"kāra's map lowering is competitive with Rust's, not ~3× better."

**That caveat has been overtaken by events, and its prediction held.** karac
`59c8d30cd` (2026-08-22) replaced the Fibonacci multiply with per-process-seeded
SipHash-1-3 in both backends — closing a real offline-collision hole, since the
old seed was a compile-time constant in the compiler's own source. So the table
above IS the equal-hash comparison now; there is no longer a fast-hash arm to
subtract. The `fasthash.rs` file is retained as history, not as a claim.

The measured outcome at genuine parity is **1.18× behind** equal-safety Rust,
against the old prediction of a tie. Close to that interval's edge, and a long
way from the ~3× lead the pre-2026-08-22 numbers showed. The honest reading is
that kāra's map is *in the same class* as `std::collections::HashMap` and
currently slightly behind it — see kara `B-2026-09-07-53` for the corpus-wide
measurement and `B-2026-09-07-42` for the bisect.

**C's 8.4× lead** comes from a fixed 512-slot stack table with no allocation,
no resizing and no rehash — still an open-addressed hash map, not a
direct-address count table.

### Compile, size, memory

| Metric | kāra | rust | c | go |
|---|---|---|---|---|
| Compile (cold) | 367.1 ms ± 25.1 | 347.0 ms ± 15.3 | 140.6 ms ± 11.2 | — |
| Binary size | **336.9 KiB** | 3886.6 KiB | 15.8 KiB | 2166.8 KiB |
| Runtime peak RSS | 2.4 MiB | 2.2 MiB | 1.7 MiB | 5.1 MiB |

`karac build` lands within 6% of `rustc -O` here, and the binary is ~11.5×
smaller than Rust's.

### Measurement caveats

- Virtualized x86-64 container; hyperfine flags outliers on several lanes even
  when idle, so the ~10–15% σ is intrinsic to the host. Read nothing into
  differences smaller than that — in particular the equal-hash lane and kāra
  are indistinguishable.
- The Python mirror is the correctness oracle, not a measured lane — it stays
  behind the `KARA_BENCH_INCLUDE_PY` gate by design.

## Kāra features exercised

- **`Map[i64, i64]` vs `Map[String, i64]`** — the same `insert` / `get` /
  `keys()` surface across the scalar and heap-half lowering fork.
- **`for k in counts.keys()`** feeding a `Vec` the loop then sorts in place.
- **Index assignment during insertion sort** (`vals[b + 1i64] = prev`) — the
  shift-right idiom, on both `Vec[i64]` and `Vec[String]`.
- **`break` out of a `while`** as the insertion-sort inner-loop exit.
- **`not` for boolean negation** (Kāra rejects `!`).
- **`.clone()` on `String` map keys** — required where a key is both looked up
  and inserted, and where a `Vec[String]` element is read out while the vector
  stays live.
- **`String.new()` + `+` accumulation** with f-string interpolation to render
  the result list.
