# Cross-origin isolation — required for SharedArrayBuffer, which enables the
# multicore (Web Worker pool) build. Without these headers the app still works
# but falls back to the single-threaded module automatically.
/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
